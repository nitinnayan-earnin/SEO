# Core dependencies
pandas>=1.5.0  # Databricks compatible (may already be installed)
langchain-openai>=0.1.0
langchain-core>=0.3.72  # Check existing version, may need <1.0.0 if langchain conflicts
requests>=2.31.0
selenium>=4.15.0
beautifulsoup4>=4.12.0
httpx>=0.25.0

# Optional: Install only if needed
# pydynamodb>=0.7.4  # Only needed for SageMaker
# boto3>=1.37.24  # Usually pre-installed in Databricks




